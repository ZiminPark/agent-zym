---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  ogDescription?: string;
  image?: string;
  type?: 'website' | 'article';
  pubDate?: Date;
  tags?: string[];
  llmContextHref?: string;
}

const { title, description, ogDescription, image, type, pubDate, tags, llmContextHref } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={title} description={description} ogDescription={ogDescription} image={image} type={type} pubDate={pubDate} tags={tags} llmContextHref={llmContextHref} />
  </head>
  <body class="min-h-screen flex flex-col">
    <Header />
    <main class="flex-1 mx-auto w-full max-w-5xl px-4 sm:px-6 py-8 sm:py-10 md:py-12">
      <slot />
    </main>
    <Footer />
  </body>
</html>
